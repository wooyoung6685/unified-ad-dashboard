import type { TikTokStatsResult } from './types'

export async function fetchTikTokStats(
  advertiserId: string,
  date: string,
  accessToken: string,
): Promise<TikTokStatsResult | null> {
  // TODO: TikTok Ads API 호출
  return null
}
