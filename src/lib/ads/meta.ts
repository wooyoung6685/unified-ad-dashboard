import type { MetaStatsResult } from './types'

export async function fetchMetaStats(
  accountId: string,
  date: string,
  accessToken: string,
): Promise<MetaStatsResult | null> {
  // TODO: Meta Graph API 호출
  return null
}
